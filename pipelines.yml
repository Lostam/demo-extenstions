resources:
  - name: jatin_gitrepo
    type: GitRepo
    configuration:
      gitProvider: jatin_gh
      path: jatindogra/demo-extenstions

pipelines:
  - name: extensions_pipelines
    steps:
      - name: extension_step
        type: playbook/ansible
        syntaxVersion: latest
        configuration:              
          foo: bar
          inputResources:
            - name: jatin_gitrepo
        execution:
          onStart:
            - echo "Preparing for work..."

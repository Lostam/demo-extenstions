resources:
#   - name: jatin_gitRepo
#     type: GitRepo
#     configuration:
#       gitProvider: jatin_gh
#       path: jatindogra/demo-extenstions
#       branches:
#         include: ^main$
 
  - name: extension_resource
    type: playbook/gitrepo
    syntaxVersion: latest
    configuration:
      GitHub : jatin_gh
      gitUrl: https://github.com/jatindogra/demo-extenstions.git
      gitBranch: master
      gitRepoName: demo-extenstions
      resourceName: extension_resource
      

pipelines:
  - name: extensions_pipelines
    steps:
      - name: extension_step
        type: playbook/ansible
        syntaxVersion: latest
        configuration:              
          foo: bar
          inputResources:
            - name: extension_resource
        execution:
          onStart:
            - echo "Preparing for work..."
            - pushd $res_extension_resource_gitRepoName/
          onComplete:
            - popd
